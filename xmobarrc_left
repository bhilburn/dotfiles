Config { font = "-*-terminus-*-*-*-*-12-*-*-*-*-*-*-u",
       bgColor = "#3f3f3f",
       fgColor = "#dcdccc",
       position = Top,
       lowerOnStart = True,
       commands = [Run StdinReader],
       sepChar = "%",
       alignSep = "}{",
       template = "%StdinReader% }{ %date%"
       }
